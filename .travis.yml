dist: trusty

install: gradle wrapper --gradle-version 7.3.1

language: java

jdk:
  openjdk11

before_script:
#  - chmod a+x gradlew
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar

script:
  - ./gradlew clean test